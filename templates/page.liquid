{{ 'onqor.page.scss' | asset_url | stylesheet_tag }}
<div class="page-width onqor-page-default">
      <div class="section-header">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
        {{ page.content }}
      </div>
</div>

{% section 'onqor-guarentee' %}
